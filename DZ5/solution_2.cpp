/*
#include <iostream>
#include <cmath>
using namespace std;

double func(double a, double b, int c){
    if (c > a+b){
        return a+b;
    }
    else{
        return fabs(a-b);
    }
}

int main() {
    double a,  b;
    int c;
    while(true){
        cout<<"Enter the first number(double):";
        cin>>a;
        cout<<"Enter the second number(double):";
        cin>>b;
        c = pow(a,b);
        cout<<"The result:"<<" "<<func(a,b,c);
        return 0;
    }
}

*/
